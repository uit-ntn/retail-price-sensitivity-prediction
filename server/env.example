# Model Configuration
# S3 bucket from Task 3 - Data Pipeline Optimization
MODEL_BUCKET=mlops-retail-prediction-dev-<your-account-id>
# SageMaker model artifacts from Task 4 - End-to-End Model Training
MODEL_KEY=artifacts/retail-price-sensitivity-model.tar.gz
# Gold dataset path for feature loading
GOLD_DATA_PREFIX=gold/
# Model version from SageMaker Model Registry  
MODEL_VERSION=1.0.0

# AWS Configuration
# Use IAM roles in production (EKS IRSA), credentials for local development only
AWS_ACCESS_KEY_ID=your-access-key-here
AWS_SECRET_ACCESS_KEY=your-secret-key-here
AWS_DEFAULT_REGION=us-east-1

# Azure Configuration (if using Azure Blob Storage)
# AZURE_STORAGE_CONNECTION_STRING=your-connection-string
# AZURE_STORAGE_ACCOUNT_NAME=your-storage-account
# AZURE_STORAGE_ACCOUNT_KEY=your-storage-key

# Data Pipeline Configuration (Task 3)
# S3 data lake structure: raw/ -> silver/ -> gold/ -> artifacts/
RAW_DATA_PREFIX=raw/
SILVER_DATA_PREFIX=silver/ 
PROCESSED_DATA_PREFIX=gold/
ARTIFACTS_PREFIX=artifacts/

# SageMaker Configuration (Task 4)
# SageMaker training job outputs and model registry  
SAGEMAKER_MODEL_NAME=retail-price-sensitivity-model
SAGEMAKER_ENDPOINT_NAME=retail-price-sensitivity-endpoint
MODEL_REGISTRY_NAME=mlops-retail-model-package-group

# Server Configuration
PORT=8000
HOST=0.0.0.0
LOG_LEVEL=INFO
ENVIRONMENT=development

# Feature Engineering
# Enable real-time feature computation from gold dataset
ENABLE_FEATURE_CACHE=true
FEATURE_CACHE_TTL=300

